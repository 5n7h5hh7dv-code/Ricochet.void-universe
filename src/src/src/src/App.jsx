export default function App() {
  return (
    <main className="page">
      <section className="hero">
        <h1>Ricochet Void Universe</h1>

        <p>
          Signal grows where noise falls.
        </p>

        <p>
          Enter the foundation archives and unlock progression.
        </p>
      </section>
    </main>
  );
}
